
import { Link } from "react-router";

export default function Counter() {
    return (
        <header>
            <Link to="/" className="text-xl">
                BRAND
            </Link>

        </header>
    );
}